---
interface Props {
  points: (string | number)[]
}

const { points } = Astro.props
---

<div class="grid grid-cols-6 gap-3">
  {
    points.map((value) => (
      <div
        class="flex items-center justify-center aspect-[2/3] rounded-lg text-3xl cursor-pointer"
        x-data={`{value: '${value}'}`}
        x-bind:class="value == room.myVote() ? 'bg-primary text-primary-text' : 'bg-secondary dark:bg-primary-dark'"
        x-on:click="value == room.myVote() ? room.unvote() : room.vote(value)"
      >
        {value}
      </div>
    ))
  }
</div>
